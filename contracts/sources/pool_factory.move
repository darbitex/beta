/// Darbitex Beta — pool factory.
///
/// Creates canonical pools (one per pair), holds hook NFT escrow listings,
/// and exposes the buy_hook entry for the escrow fixed-price sale. This
/// file is currently a stub — full implementation in Task #12.

module darbitex::pool_factory {
    // Intentionally empty — this stub exists so that pool.move's
    // `friend darbitex::pool_factory` declaration resolves during compile
    // while the full implementation is still being written.
}
